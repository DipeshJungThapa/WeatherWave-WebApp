# 1) Python / Django ignores
__pycache__/
*.py[cod]
*.egg
*.egg-info/
build/
dist/
venv/
.env
db.sqlite3
*.log

# 2) Node / Frontend ignores (if you use React/Node later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error*

# 3) macOS / Windows junk
.DS_Store
Thumbs.db

# ML Specific
# Ignore all data files (raw and processed) as they are generated by scripts
ml/data/
# Ignore the entire models directory where trained models and encoders are saved.
ml/models/